<%= form_for(termino) do |f| %>
  <% if termino.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(termino.errors.count, "error") %> prohibited this termino from being saved:</h2>

      <ul>
      <% termino.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
