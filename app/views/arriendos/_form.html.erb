<%= form_for(arriendo) do |f| %>
  <% if arriendo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(arriendo.errors.count, "error") %> prohibited this arriendo from being saved:</h2>

      <ul>
      <% arriendo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% if user_signed_in? %>
    <div class="field">
      <%= f.label "Fecha de Uso" %>
      <%= f.date_select :fecha, {order: [:day, :month, :year], prompt: {day: 'Dia', month: 'Mes', year: 'Año'}, start_year: Date.today.year, end_year: Date.today.year + 5}, :required => true %>
    </div>

    <div class="field">
      <%= f.label "Dias de Uso" %>
      <%= f.number_field :dias %>
    </div>

    <div class="field">
      <%= f.label "Nivel" %>
      <%= f.number_field :dias, :min => 1, :max => 3, :value => current_user.experience %>
    </div>

    <div class="field">
      <%= f.label 'Talla de bota' %>
      <%= f.number_field :talla, :value => current_user.talla, :min => 1 %>
    </div>

    <div class="field">
      <%= f.label 'Talla de parka' %>
      <%= f.number_field :talla_parka, :value => current_user.talla_parka, :min => 1 %>
    </div>

    <div class="field">
      <%= f.label 'Talla de pantalon' %>
      <%= f.number_field :talla_pantalon, :value => current_user.talla_pantalon, :min => 1 %>
    </div>

    <div class="field">
      <%= f.label :altura %>
      <%= f.number_field :altura, :value => current_user.altura, :min => 1 %>
    </div>

    <div class="field">
      <%= f.label :peso %>
      <%= f.number_field :peso, :value => current_user.peso, :min => 1 %>
    </div>

    <div class="field">
      <%= f.hidden_field :cantidad, :value => 1 %>
    </div>

    <div class="field">
      <%= f.hidden_field :user_id, :value => current_user.id %>
    </div>

    <div class="field">
      <%= f.hidden_field :equipo_id, :value => id %>
    </div>

    <% if params[:arr].present? %>
      <div class="field">
        <%= f.hidden_field :nropedido, :value => Arriendo.find(params[:arr]).nropedido %>
      </div>
    <% else %>
      <div class="field">
        <%= f.hidden_field :nropedido, :value => Time.current.to_i.to_s + Equipo.find(id).name[0..2].upcase + id.to_s%>
      </div>
    <% end %>

    <div class="actions">
      <%= f.submit %>
    </div>
  <% else %>

  <p>
    Si te registras, guardamos tus datos y no tienes que rellenarlos siempre!
  </p>

  <br>
  </br>

    <div class="field">
      <%= f.label :email %>
      <%= f.email_field :email, :required => true %>
    </div>

    <div class="field">
      <%= f.label :nombre %>
      <%= f.text_field :nombre %>
    </div>

    <div class="field">
      <%= f.label 'Apellido Paterno' %>
      <%= f.text_field :apellidop %>
    </div>

    <div class="field">
      <%= f.label 'Apellido Materno' %>
      <%= f.text_field :apellidom %>
    </div>

    <div class="field">
      <%= f.hidden_field :cantidad, :value => 1 %>
    </div>

    <div class="field">
      <%= f.label 'Talla de bota' %>
      <%= f.number_field :talla, :min => 1 %>
    </div>

    <div class="field">
      <%= f.label 'Talla de parka' %>
      <%= f.number_field :talla_parka, :min => 1 %>
    </div>

    <div class="field">
      <%= f.label 'Talla de pantalon' %>
      <%= f.number_field :talla_pantalon, :min => 1 %>
    </div>

    <div class="field">
      <%= f.label :altura %>
      <%= f.number_field :altura, :min => 1 %>
    </div>

    <div class="field">
      <%= f.label :peso %>
      <%= f.number_field :peso, :min => 1 %>
    </div>

    <div class="field">
      <%= f.label "Fecha de Uso" %>
      <%= f.date_select :fecha, {order: [:day, :month, :year], prompt: {day: 'Dia', month: 'Mes', year: 'Año'}, start_year: Date.today.year, end_year: Date.today.year + 5}, :required => true %>
    </div>

    <div class="field">
      <%= f.label "Dias de Uso" %>
      <%= f.number_field :dias, :min => 1 %>
    </div>

    <div class="field">
      <%= f.label "Nivel de ski" %>
      <%= f.number_field :dias, :min => 1, :max => 3, :value => 1 %>
    </div>

    <div class="field">
      <%= f.hidden_field :equipo_id, :value => id %>
    </div>

    <div class="field">
      <%= f.hidden_field :nropedido, :value => Time.current.to_i.to_s + Equipo.find(id).name[0..2].upcase + id.to_s %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
<% end %>
